package main

import "fmt"

func main() {
	var T int
	fmt.Scan(&T)
	for ; T > 0; T-- {
		var N, K int
		fmt.Scan(&N, &K)
		for i := 0; i < N; i++ {
			var M int
			fmt.Scan(&M)
			for j := 0; j < M; j++ {
			}
		}
	}
}
